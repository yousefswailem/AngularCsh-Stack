export interface Customer {
    id: number;
    firstName?: string;
    lastName?: string;
    address?: string;
    phone?: string;
    email?: string;
    // orders: Order[];
  }
  
  // export interface Order {
  //   id: number;
  // }